import type { Config } from 'tailwindcss'

export default {
  theme: {
    extend: {
      fontFamily: {
        game: ['"JetBrains Mono"', 'ui-monospace', 'monospace'],
      },
    },
  },
} satisfies Config
